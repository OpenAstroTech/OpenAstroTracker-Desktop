<Window x:Class="OATControl.RenameSlewPointDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Position"
        Height="118" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
		BorderThickness="1"
		BorderBrush="#300"
        ShowInTaskbar="False">
	<Window.Resources>
		<Style x:Key="TextBorder" TargetType="TextBox" BasedOn="{StaticResource MetroTextBox}">
			<Setter Property="BorderBrush" Value="{StaticResource AccentColorBrush}" />
			<Setter Property="Background" Value="{StaticResource AccentColorBrush4}" />
			<Setter Property="HorizontalContentAlignment" Value="Left" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="FontSize" Value="14" />
			<Setter Property="MinWidth" Value="50" />
			<Setter Property="Foreground" Value="#B00" />
		</Style>
		<Style x:Key="TextBlockLabel" TargetType="TextBlock" BasedOn="{StaticResource MetroTextBlock}">
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="FontSize" Value="13" />
			<Setter Property="Margin" Value="0,0" />
			<Setter Property="Padding" Value="0,3" />
			<Setter Property="HorizontalAlignment" Value="Left" />
			<Setter Property="Foreground" Value="#B00" />
		</Style>

	</Window.Resources>
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<TextBlock Text="Enter new name:" Style="{StaticResource TextBlockLabel}" />
		<TextBox x:Name="nameTextBox" Grid.Row="1" Margin="0,0,0,15"  Style="{StaticResource TextBorder}" Text="{Binding PointName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="OK" MinWidth="75" Margin="0,0,2,0" Style="{StaticResource AccentedSquareButtonStyle}" IsDefault="True" Click="OkButton_Click"/>
			<Button Content="Cancel" MinWidth="75" Margin="0,0,2,0" Style="{StaticResource AccentedSquareButtonStyle}" IsCancel="True" />
		</StackPanel>
	</Grid>
</Window>
